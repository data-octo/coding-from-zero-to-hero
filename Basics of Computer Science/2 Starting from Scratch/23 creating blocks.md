To avoid repeating the same set of blocks over and over again, its possible to take a shortcut by creating new blocks. Each new block can contain several different instructions.

- Making you own block
- Blocks with inputs
